@model WA_Blogers_MVC.Models.WA_Blogs

@{
    ViewBag.Title = "ChiTietBlog";
    Layout = "~/Views/Shared/Admin/Layout.cshtml";
}

<h2><b>Chi tiết Blog</b></h2>

<table class="table table-bordered table-striped">
    <tr>
        <th>Tên Blog</th>
        <td>@Html.DisplayFor(model => model.Name)</td>
    </tr>
    <tr>
        <th>Parent</th>
        <td>@Html.DisplayFor(model => model.WA_Blogs2.Name)</td>
    </tr>
    <tr>
        <th>Order</th>
        <td>@Html.DisplayFor(model => model.Order)</td>
    </tr>
    <tr>
        <th>Active</th>
        <td>@Html.CheckBoxFor(modelItem => modelItem.Active, new { data_toggle = "toggle", disable = true })</td>
        <th></th>
    </tr>
</table>
    @*<p><div class="form-actions no-color">
           <a type="button" class="btn btn-block btn-warning " href="@Url.Action("Edit" , new { id = Model.BlogID })"><i class="fa fa-pencil" aria-hidden="true"></i> Sửa</a>
    @Html.ActionLink("Sửa", "Edit", new { id = Model.BlogID }, new { @class = "btn btn-warning" })
    @Html.ActionLink("Xóa", "Delete", new { id = Model.BlogID }, new { @class = "btn btn-danger" })
    @Html.ActionLink("Quay lại", "Index", null, new { @class = "btn btn-default" })
</div></p>*@

<p>
   
    <div class="form-group form-inline">
        <div class="col-xs-2" style="padding:2px">
            <a type="button" class="btn btn-block btn-warning " href="@Url.RouteUrl("ChinhSuaBlog", new { id = Model.BlogID, title=UrlEncode.ToFriendlyUrl(Model.Name) })"><i class="fa fa-pencil" aria-hidden="true"></i> Sửa</a>
        </div>
        <div class="col-xs-2" style="padding:2px">
            <a type="button" href="@Url.RouteUrl("XoaBlog", new { id = Model.BlogID, title=UrlEncode.ToFriendlyUrl(Model.Name) })" class="btn btn-block btn-danger" id="btnDelete"><i class="fa fa-trash" aria-hidden="true"></i> Xóa</a>
        </div>
        <div class="col-xs-2" style="padding:2px">
            <a href="@Url.RouteUrl("QuanLyBlog") " type=" button" class="btn btn-block btn-default col-xs-2" data-dismiss="modal">Quay lại</a>
        </div>
    </div>
</p>
